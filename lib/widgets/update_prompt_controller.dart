// 更新提示控制器（最小存根，保留原 API 表面）
import 'package:flutter/widgets.dart';

class UpdatePromptController {
  UpdatePromptController._();

  /// 检查应用更新。manual=true 时表示用户手动触发。
  static Future<void> check(BuildContext context, {bool manual = false}) async {
    // 存根：暂不实现更新检查逻辑。
  }
}
